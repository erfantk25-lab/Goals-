apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  namespace: goalplanner
type: Opaque
stringData:
  # Do not commit actual secrets! Use external secret operators or inject via CI/CD.
  POSTGRES_PASSWORD: "YOUR_DB_PASSWORD_HERE"
  OPENAI_API_KEY: "YOUR_OPENAI_API_KEY_HERE"
